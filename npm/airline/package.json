{
  "name": "airline",
  "version": "0.0.0",
  "private": true,
  "_comment": "/*this is to start the server, by typing 'node ./bin/www' in command line?*,
               WRONG, just type 'npm start' and it starts 'node ./bin/www'",
  "scripts": {
    "start": "node ./bin/www"
  },
  "dependencies": {
    "body-parser": "~1.13.2",
    "cookie-parser": "~1.3.5",
    "debug": "~2.2.0",
    "express": "~4.13.1",
    "morgan": "~1.6.1",
    "serve-favicon": "~2.3.0"
  }
}
